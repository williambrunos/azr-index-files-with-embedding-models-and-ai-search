from setuptools import setup, find_packages
setup(
    name = 'azuresearchclient',
    packages = find_packages(),
    version='0.1.0',
    description='A client for Azure Search',
    author='William'
)